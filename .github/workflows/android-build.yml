name: 🚀 Build Android AAB

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:  # Permet de lancer manuellement

jobs:
  build-aab:
    name: 📱 Générer AAB
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: ☕ Setup Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: 📦 Setup Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: 🔧 Install dependencies
      run: npm ci
    
    - name: 🏗️ Build web app
      run: npm run build
    
    - name: 🔄 Sync Capacitor
      run: npx cap sync android
    
    - name: 🎯 Build AAB (Debug - Non Signé pour Test)
      run: |
        cd android
        chmod +x gradlew
        ./gradlew bundleDebug
    
    - name: 📤 Upload AAB
      uses: actions/upload-artifact@v4
      with:
        name: camionback-app-debug
        path: android/app/build/outputs/bundle/debug/app-debug.aab
        retention-days: 30
    
    - name: 📊 AAB Info
      run: |
        ls -lh android/app/build/outputs/bundle/debug/app-debug.aab
        echo "✅ AAB DEBUG généré avec succès !"
        echo "⚠️  IMPORTANT: Ceci est un AAB de DEBUG (non signé)"
        echo "📝 Pour PRODUCTION: Créez un keystore et signez l'AAB"
        echo "📥 Téléchargez-le depuis l'onglet 'Actions' de GitHub"
